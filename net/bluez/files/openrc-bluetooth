#!/sbin/openrc-run
description="Bluetooth daemon"
command="/usr/libexec/bluetooth/bluetoothd"
command_args=""
command_background="yes"
pidfile="/run/bluetooth.pid"

depend() {
    need dbus
    after dbus
}
