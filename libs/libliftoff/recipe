name        libliftoff
version     0.5.0
release     1
description libliftoff: KMS plane library

source      https://gitlab.freedesktop.org/emersion/libliftoff/-/releases/v0.5.0/downloads/libliftoff-0.5.0.tar.gz
hash        sha256:e29af0c9dc765916c1f192f118efffc947af6600178b04d5bccca1901246f83b

depend      filesystem
depend      libdrm
builddep    meson
builddep    ninja

configure   meson setup build --prefix=/usr --libdir=lib64 --buildtype=release
build       meson compile -C build -j "$MAKEJOBS"
install     DESTDIR="$PKG" meson install -C build
